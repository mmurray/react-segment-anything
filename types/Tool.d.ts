import React from "react";
import { ToolProps } from "./helpers/Interfaces";
declare const Tool: ({ handleClicksChange, handleBoundingBoxChange, image, maskImage, bbox, clicks, mode }: ToolProps) => React.JSX.Element;
export default Tool;
